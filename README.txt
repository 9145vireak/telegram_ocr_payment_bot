README - Telegram Bot OCR Auto Payment

This bot accepts /start command, shows QR code, and verifies payment slip by OCR.

Build Command:
pip install -r requirements.txt

Start Command:
python main.py

Deploy as: Background Worker on Render
